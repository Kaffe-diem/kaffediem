services:
  pb:
    volumes:
      - ./pocketbase/pb_data:/app/pb_data
    ports:
      - "8080:8080"
  app:
    environment:
      - PUBLIC_PB_HOST=http://localhost:8080
    ports:
      - "5173:5173" 